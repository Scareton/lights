from flask_wtf import Form
from wtforms import TextField, IntegerField, TextAreaField, SubmitField, RadioField, SelectField

class ProductForm(Form, details, count):
    name = TextField("Имя товара",[validators.Required("Введите имя товара")])
    components = []
    
    while count:

